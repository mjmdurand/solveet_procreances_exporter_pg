SELECT
    RGCLEUNIK,
    SUBSTRING(NUMDEB, 1, 20) AS NUMDEB,
    SUBSTRING(NUMCLI, 1, 20) AS NUMCLI,
    DFCLEUNIK,
    SUBSTRING(NUMINT, 1, 20) AS NUMINT,
    SUBSTRING(NUMPAI, 1, 20) AS NUMPAI,
    SUBSTRING(LIBELLE, 1, 250) AS LIBELLE,
    MONTANT,
    DATESAIS,
    DATEREGL,
    SUBSTRING(MODREG, 1, 20) AS MODREG,
    MTIMP,
    DATIMP,
    SUBSTRING(COMPLEX, 1, 1) AS COMPLEX,
    SUBSTRING(CCLI, 1, 1) AS CCLI,
    SUBSTRING(ORDCLI, 1, 1) AS ORDCLI,
    REGTXINT,
    SUBSTRING(NUMPRINC, 1, 30) AS NUMPRINC,
    SUBSTRING(COLCD, 1, 1) AS COLCD,
    SUBSTRING(TYPREG, 1, 20) AS TYPREG,
    SUBSTRING(VALIDE, 1, 1) AS VALIDE,
    SUBSTRING(RGBANQUE, 1, 100) AS RGBANQUE,
    SUBSTRING(RGLIEUPAIE, 1, 100) AS RGLIEUPAIE,
    SUBSTRING(RGHORSDEP, 1, 1) AS RGHORSDEP,
    BOCLEUNIK,
    TRNUMMVT,
    TRNUMMVTIMP
FROM REG;
