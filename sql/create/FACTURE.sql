create table FACTURE (
    FANUMFACT VARCHAR(20) PRIMARY KEY,
    NUMCLIENT VARCHAR(20) NOT NULL,
    NUMDOSSIER VARCHAR(20),
    REFCLIENT VARCHAR(50),
    TOTREVERTTC DECIMAL(10, 2) NOT NULL,
    TOTDISPO DECIMAL(10, 2) NOT NULL,
    TOTRGLT DECIMAL(10, 2) NOT NULL,
    FADATEDIT DATE,
    FADATEECH DATE,
    TOTMTHONO DECIMAL(10, 2) NOT NULL,
    FAMTTVA DECIMAL(10, 2) NOT NULL,
    HTREGLCLIENT DECIMAL(10, 2) NOT NULL,
    HTREGDISPO DECIMAL(10, 2) NOT NULL,
    MTTTC DECIMAL(10, 2) NOT NULL,
    TYPFACT VARCHAR(20) NOT NULL,
    TRNUMMVT int,
    FAMODREG char(3),
    FACONDPAIE VARCHAR(250),
    FADATECREA DATE
);